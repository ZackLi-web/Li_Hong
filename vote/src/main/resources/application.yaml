server:
  port: 8082
  servlet:
    context-path: /vote
    encoding:
      charset: UTF-8
      enable: true
      force: true
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/voting_system?characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Taipei&allowPublicKeyRetrieval=true
    username: Zack
    password: LeeLee86930
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect


